#pragma once

enum class SeatClass { ECONOMY, BUSINESS };
enum class SeatStatus { AVAILABLE, BOOKED };
enum class UserRole { PASSENGER, ADMIN };
enum class BookingStatus { CONFIRMED, CANCELLED, PENDING };
enum class PaymentStatus { SUCCESS, FAILED, PENDING };
